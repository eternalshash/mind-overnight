

// Attribute to hide warnings for unsued code 

#![allow(dead_code)]

#[derive(Debug)]
struct Person {
    name String,
    age: u8,
}

// A unit structure
struct Unit;

/*
Tuple structs, just named tuples
Classic C structures
Unit structs, which are field-less and useful for any generics 

*/
